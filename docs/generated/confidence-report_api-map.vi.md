---
source_artifact: docs/generated/api-map.md
claims_total: 1
claims_with_evidence: 0
confidence_derived: 0.0
generated_by: derive_confidence_report.py
---

# Báo cáo độ tin cậy -- docs/generated/api-map.md

> **Số liệu độ phủ trích dẫn tự báo cáo -- KHÔNG phải kiểm chứng đúng/sai.** Báo cáo này được tạo tự động bằng cách parse các trích dẫn `**Source:** file:line` và các tag đánh dấu `[UNVERIFIED]`/`[INFERRED]`/`[NEEDS_DOMAIN_CONFIRMATION]` ngay trong tài liệu. Nó KHÔNG xác minh trích dẫn có chính xác hay khẳng định có đúng hay không. Muốn kiểm chứng độc lập, xem `claude/skills/audit-doc-parity/`.

## Khẳng định ↔ Bằng chứng

Chú giải: `○` = có trích dẫn (có `Source: file:line`) · `△` = có đánh dấu (chưa chắc chắn, không có trích dẫn).

| Khẳng định | Mục | Bằng chứng (file:line) | Trạng thái ○/△ |
|---|---|---|---|
| Concerns/Blockers:** (1) Cột Auth dùng thuật ngữ tên guard ("public"/"Bearer"), không phải mã PERM### c… | Webhooks / External Calls | — | △ |

## Thông tin còn thiếu

Các mục nên kiểm tra thêm khẳng định `△` (được đánh dấu) -- chỉ mang tính tham khảo, không phải kết luận chính thức:

- Webhooks / External Calls: Concerns/Blockers:** (1) Cột Auth dùng thuật ngữ tên guard ("public"/"Bearer"), không phải mã PERM### c…

## Cảnh báo rủi ro

- Độ phủ trích dẫn thấp (0%) -- phần lớn khẳng định chỉ được đánh dấu, không có trích dẫn.
