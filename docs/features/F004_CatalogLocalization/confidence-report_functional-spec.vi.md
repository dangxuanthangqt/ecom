---
source_artifact: docs/features/F004_CatalogLocalization/functional-spec.md
claims_total: 1
claims_with_evidence: 0
confidence_derived: 0.0
generated_by: derive_confidence_report.py
---

# Báo cáo độ tin cậy -- docs/features/F004_CatalogLocalization/functional-spec.md

> **Chỉ số độ phủ trích dẫn tự báo cáo -- KHÔNG phải kiểm chứng tính đúng đắn.** Báo cáo này được tạo tự động bằng cách parse các trích dẫn nội tuyến `**Source:** file:line` và các thẻ đánh dấu `[UNVERIFIED]`/`[INFERRED]`/`[NEEDS_DOMAIN_CONFIRMATION]` trong chính artifact. Nó KHÔNG kiểm chứng trích dẫn có chính xác hay khẳng định có đúng hay không. Để kiểm chứng độc lập, xem `claude/skills/audit-doc-parity/`.

## Khẳng định ↔ Bằng chứng

Chú giải: `○` = có trích dẫn (có Source file:line) · `△` = có gắn thẻ đánh dấu (chưa chắc chắn, không có trích dẫn).

| Khẳng định | Mục | Bằng chứng (file:line) | Trạng thái ○/△ |
|---|---|---|---|
| [ ] Admin được cảnh báo rằng mọi bản dịch vẫn đang trỏ đến ngôn ngữ này sẽ bị xóa cùng lúc… | 7. User Stories | — | △ |

## Thông tin còn thiếu

Các mục cần kiểm tra thêm với `△` (khẳng định gắn thẻ đánh dấu) -- chỉ mang tính tham khảo, không phải kết luận cuối cùng:

- 7. User Stories: [ ] Admin được cảnh báo rằng mọi bản dịch vẫn đang trỏ đến ngôn ngữ này sẽ bị xóa cùng lúc…

## Cảnh báo rủi ro

- Độ phủ trích dẫn thấp (0%) -- phần lớn khẳng định chỉ được gắn thẻ đánh dấu, không có trích dẫn.
